version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: swipesmart-postgres
    environment:
      POSTGRES_USER: swipesmart
      POSTGRES_PASSWORD: password
      POSTGRES_DB: swipesmart_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U swipesmart']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: swipesmart-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
  redis_data:
